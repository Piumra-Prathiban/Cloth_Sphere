spring:
  datasource:
    url: jdbc:sqlserver://localhost:1433;databaseName=clothsphere;encrypt=true;trustServerCertificate=true
    username: sa
    password: 12
    driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.SQLServerDialect

server:
  port: 8080
  
